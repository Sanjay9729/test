[build]
  command = "CI=false npm run build"
  publish = "build"
  functions = "netlify/functions"
  targetPort = 3000
  port = 8888
[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"
